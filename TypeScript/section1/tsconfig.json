{
  // 컴파일 결과 생성되는 파일 관련 옵션
  "compilerOptions": {
    // ESNext: JS 최신버전을 의미
    "target": "ESNext",
    "module": "ESNext",
    // 컴파일 결과가 'dist' 폴더에 생성됨
    "outDir": "dist",
    // 타입 검사를 엄격할 지 여부를 관리
    "strict": true,
    //  ts가 각각의 파일을 어떤 모듈로 감지할 것인지 정해주는 옵션
    "moduleDetection": "force"
  },
  "ts-node": {
    "esm": true
  },
  // src 하위의 모든 파일들을 컴파일함
  "include": ["src"]
}
